/*interface UserTG {
    name: string;
    login: string;
    email: string;
}

const userTG: UserTG = {
    name: 'Vasya',
    login: 'vasya',
    email: 'XGjC0@example.com'
}

interface Admin {
    name: string;
    role: number
}

function logIdTG(id: string | number) {
    if (isString(id)) {
        console.log(id);
    } else {
        console.log(id);
    }
}

function isString(x: string | number): x is string {
    return typeof x === 'string';
}*/